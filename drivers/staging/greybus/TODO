* Convert all uses of the old GPIO API from <linaos/gpio.h> to the
  GPIO descriptor API in <linaos/gpio/consumer.h> and look up GPIO
  lines from device tree or ACPI.
* Make pwm.c use the struct pwm_ops::apply instead of ::config, ::set_polarity,
  ::enable and ::disable.
