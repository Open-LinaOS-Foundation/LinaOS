name: kernel
version: KERNELRELEASE
summary: LinaOS kernel
description: The upstream LinaOS kernel
grade: stable
confinement: strict
type: kernel

parts:
  kernel:
    plugin: kernel
    source: SRCTREE
    source-type: tar
    kernel-with-firmware: false
